import { CommonModule } from '@angular/common';import { Component,OnInit,inject } from '@angular/core';import { FormsModule } from '@angular/forms';import { ApiService } from '../../core/api.service';import { Vehicle } from '../../models/crm.models';import { TransportNavComponent } from './transport-nav.component';
@Component({standalone:true,imports:[CommonModule,FormsModule,TransportNavComponent],template:`<section class="page-head"><div><p class="eyebrow">Transport Management</p><h1>Vehicles</h1><p class="page-copy">Add vehicles and control which ones can be assigned to visits.</p></div><button (click)="showForm=!showForm">{{showForm?'Close form':'+ Add vehicle'}}</button></section><app-transport-nav/>
<article class="panel form-card" *ngIf="showForm"><div class="section-title"><span>+</span><div><h2>Register new vehicle</h2><p>Enter the vehicle identity and capacity.</p></div></div><div class="form-grid"><label class="field"><span>Registration number *</span><input [(ngModel)]="draft.registrationNumber" placeholder="DHAKA METRO-GA-00-0000"></label><label class="field"><span>Vehicle type *</span><select [(ngModel)]="draft.vehicleType"><option value="">Select vehicle type</option><option *ngFor="let t of types">{{t}}</option></select></label><label class="field"><span>Brand *</span><input [(ngModel)]="draft.brand" placeholder="e.g. Toyota"></label><label class="field"><span>Model *</span><input [(ngModel)]="draft.model" placeholder="e.g. Noah"></label><label class="field"><span>Color</span><select [(ngModel)]="draft.color"><option value="">Select color</option><option *ngFor="let c of colors">{{c}}</option></select></label><label class="field"><span>Seating capacity *</span><input type="number" min="1" max="100" [(ngModel)]="draft.seatingCapacity"></label></div><p class="error" *ngIf="error">{{error}}</p><div class="form-actions"><button class="ghost-button" (click)="clear()">Clear</button><button (click)="add()">Save vehicle</button></div></article>
<div class="vehicle-grid"><article class="vehicle-card" *ngFor="let v of vehicles"><div class="vehicle-card-head"><div class="vehicle-icon">{{icon(v.vehicleType)}}</div><span class="status-pill" [class.approved]="v.isActive" [class.inactive]="!v.isActive">{{v.isActive?'Active':'Inactive'}}</span></div><h2>{{v.brand}} {{v.model}}</h2><strong class="registration">{{v.registrationNumber}}</strong><div class="vehicle-meta"><span>{{v.vehicleType}}</span><span>{{v.color||'Color not set'}}</span><span>{{v.seatingCapacity}} seats</span></div><button [class.danger]="v.isActive" [class.ghost-button]="!v.isActive" (click)="toggle(v)">{{v.isActive?'Deactivate vehicle':'Activate vehicle'}}</button></article><div class="empty-card" *ngIf="!vehicles.length">No vehicles registered yet.</div></div>`})
export class VehiclesComponent implements OnInit{private api=inject(ApiService);vehicles:Vehicle[]=[];showForm=false;error='';types=['Sedan','SUV','Microbus','Minivan','Pickup','Bus','Other'];colors=['Black','White','Silver','Gray','Blue','Red','Green','Brown','Other'];draft:any={};ngOnInit(){this.load();this.clear()}load(){this.api.vehicles().subscribe(v=>this.vehicles=v)}clear(){this.draft={registrationNumber:'',vehicleType:'',brand:'',model:'',color:'',seatingCapacity:4,isActive:true};this.error=''}add(){if(!this.draft.registrationNumber.trim()||!this.draft.vehicleType||!this.draft.brand.trim()||!this.draft.model.trim()){this.error='Complete all required fields.';return}this.api.createVehicle(this.draft).subscribe({next:()=>{this.clear();this.showForm=false;this.load()},error:e=>this.error=e.error?.message||'Unable to save vehicle.'})}toggle(v:Vehicle){this.api.setVehicleStatus(v.id,!v.isActive).subscribe(()=>this.load())}icon(t:string){return t==='Sedan'?'S':t==='SUV'?'U':t==='Microbus'?'M':t==='Bus'?'B':'V'}}
