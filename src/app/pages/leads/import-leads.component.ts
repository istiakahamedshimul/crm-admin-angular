import { CommonModule } from '@angular/common';import { Component, inject } from '@angular/core';import { FormsModule } from '@angular/forms';import { RouterLink } from '@angular/router';import { ApiService } from '../../core/api.service';
@Component({standalone:true,imports:[CommonModule,FormsModule,RouterLink],template:`<section class="page-head"><div><p class="eyebrow">Bulk ingestion</p><h1>Import Leads</h1><p class="page-copy">Upload a validated CSV or Excel workbook into the sales pipeline.</p></div><a routerLink="/leads" class="back">← Lead directory</a></section><section class="import-card"><header><span>⇧</span><div><h2>Upload lead file</h2><p>CSV, XLS or XLSX · maximum 10 MB</p></div></header><input type="file" #picker accept=".csv,.xls,.xlsx" hidden (change)="choose($event)"><button type="button" class="dropzone" (click)="picker.click()"><span>▤</span><strong>{{file?.name||'Choose a lead file'}}</strong><small>{{file?((file.size/1024)|number:'1.0-1')+' KB · Click to replace':'Browse from your computer'}}</small></button><label class="auto"><input type="checkbox" [(ngModel)]="autoAssign"> <span><strong>Automatically distribute leads</strong><small>Assign imported records equally among active sales employees.</small></span></label><div class="requirements"><strong>Required columns</strong><span>Name and Phone are mandatory. Project and property type are resolved automatically.</span><a [href]="templateUrl" target="_blank">Download import template →</a></div><p class="result" *ngIf="message">{{message}}</p><footer><button type="button" class="clear" (click)="file=undefined" [disabled]="!file">Clear</button><button type="button" (click)="upload()" [disabled]="!file||uploading">{{uploading?'Importing…':'Import Leads'}}</button></footer></section>`,styles:[`:host{display:block}.back{padding:10px 14px;border:1px solid var(--line);border-radius:10px;background:#fff;color:#334155;font-weight:700}.import-card{max-width:720px;margin:auto;padding:24px;border:1px solid var(--line);border-radius:18px;background:#fff;box-shadow:var(--shadow)}.import-card header{display:flex;align-items:center;gap:11px;padding-bottom:17px;border-bottom:1px solid var(--line)}.import-card header>span{display:grid;place-items:center;width:40px;height:40px;border-radius:11px;background:#e6f4f2;color:#0f766e;font-size:20px;font-weight:900}.import-card h2,.import-card p{margin:0}.import-card header p{margin-top:3px;color:#64748b;font-size:11px}.dropzone{display:grid;justify-items:center;width:100%;padding:38px;margin:20px 0;border:2px dashed #cbd5e1;border-radius:14px;background:#f8fafc;color:#334155;box-shadow:none}.dropzone:hover{border-color:#0f766e;background:#f0fdfa}.dropzone>span{font-size:30px}.dropzone strong{margin:8px 0 3px}.dropzone small{color:#64748b}.auto{display:flex;align-items:flex-start;gap:10px;padding:13px;border:1px solid var(--line);border-radius:11px}.auto input{width:auto}.auto span,.auto strong,.auto small{display:block}.auto small{margin-top:3px;color:#64748b;font-size:10px}.requirements{display:grid;gap:4px;padding:14px;margin-top:14px;border-radius:11px;background:#eff6ff;color:#1e40af;font-size:11px}.requirements span{color:#475569}.requirements a{margin-top:4px;font-weight:800}.result{padding:11px;margin-top:14px!important;border-radius:9px;background:#ecfdf5;color:#047857;font-size:11px}.import-card footer{display:flex;justify-content:flex-end;gap:8px;padding-top:18px;margin-top:18px;border-top:1px solid var(--line)}.clear{background:#fff;color:#475569;border:1px solid var(--line)}`]})
export class ImportLeadsComponent{private api=inject(ApiService);file?:File;autoAssign=false;uploading=false;message='';templateUrl=this.api.leadImportTemplateUrl();choose(event:Event){this.file=(event.target as HTMLInputElement).files?.[0];this.message='';}upload(){if(!this.file)return;this.uploading=true;this.api.importLeads(this.file,this.autoAssign).subscribe({next:result=>{this.uploading=false;this.message=`${result.imported} leads imported; ${result.skipped.length} skipped.`;this.file=undefined;},error:error=>{this.uploading=false;this.message=error.error?.message||'Import failed.';}});}}
